const root = document.getElementById('window');

const loginElement = document.createElement('a');
loginElement.href = '/login';
loginElement.textContent = 'Вход';
loginElement.className()

const signupElement = document.createElement('a');
signupElement.href = '/signup';
signupElement.textContent = 'Регистрация';

root.appendChild(loginElement);